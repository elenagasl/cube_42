{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/cub3d",
            "args": ["${input:programArgs}"],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "build"
        }
    ],
    "inputs": [
        {
            "id": "programArgs",
            "type": "pickString",
            "description": "Select program arguments",
            "options": ["./maps/test.cub"],
        }
    ]
}
